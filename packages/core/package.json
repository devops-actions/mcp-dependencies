{
  "name": "@mcp-dependencies/core",
  "version": "0.1.0",
  "description": "Core discovery and conversion logic for MCP and APM dependencies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "js-yaml": "^4.1.0",
    "jsonc-parser": "^3.3.1"
  },
  "optionalDependencies": {
    "mcpsense": "^0.3.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "vitest": "^4.1.8"
  }
}
