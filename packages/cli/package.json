{
  "name": "@mcp-dependencies/cli",
  "version": "0.1.0",
  "description": "CLI tool for discovering and reporting MCP and APM dependencies",
  "bin": {
    "mcp-apm-scan": "./dist/index.js"
  },
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --loader ts-node/esm src/index.ts",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@mcp-dependencies/core": "*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
